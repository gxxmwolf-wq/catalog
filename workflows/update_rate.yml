--- НАЧАЛО КОДА РОБОТА --- name: Update Exchange Rate

on: schedule: - cron: '0 8 * * *' workflow_dispatch:

jobs: update-rate: runs-on: ubuntu-latest permissions: contents: write steps: - name: Checkout uses: actions/checkout@v3

--- КОНЕЦ КОДА РОБОТА ---
